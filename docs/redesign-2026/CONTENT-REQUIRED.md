# R.Pay — content and permissions required

**Generated from `content/claims.json` by `scripts/check-content.mjs --write-checklist`.**
Do not edit this file by hand; edit the registry and regenerate, so the checklist and the
site can never drift apart.

_Last generated against 19 declared claims._

---

## How to read this

Every public factual statement on the site is declared in one registry. A claim is
**publishable** only when it is both `verified` and carries an `asOf` measurement date —
verified-without-a-date counts as unverified on purpose, because the date is half of what
makes a number honest.

Until then the interface renders the **fallback**, not a blank. Nothing here stops the site
being built; several items stop it being published.

- **0** resolved
- **17** unresolved and blocking launch
- **0** unresolved and blocking implementation
- **2** unresolved but safe to ship as-is

Run `node scripts/check-content.mjs --production` to fail a build while any launch blocker
is outstanding.

---

## Blocks public launch

These must be resolved before the site goes live.


### Total machines under management

| | |
|---|---|
| **Claim id** | `fleet.machines` |
| **Appears in** | Hero horizon, hero gauges, fleet section |
| **Why it is needed** | The horizon draws one peg per machine. The number IS the picture. |
| **Current dev value** | 97 machines — **DEVELOPMENT PLACEHOLDER** |
| **Measurement date** | **required — not supplied** |
| **Source / verification** | **required — not supplied** |
| **Blocks implementation** | No |
| **Blocks public launch** | **Yes** |
| **Fallback if never supplied** | Drop the exact count. Draw the horizon from branch structure only and remove the N/M gauge. |

---

### Machines currently reporting online

| | |
|---|---|
| **Claim id** | `fleet.online` |
| **Appears in** | Hero gauges (90/97), tick colour split |
| **Why it is needed** | The seven grey pegs are the most credible thing on the page. A marketing site shows 97/97. |
| **Current dev value** | 90 machines — **DEVELOPMENT PLACEHOLDER** |
| **Measurement date** | **required — not supplied** |
| **Source / verification** | **required — not supplied** |
| **Blocks implementation** | No |
| **Blocks public launch** | **Yes** |
| **Fallback if never supplied** | Remove the online split and show machines only. The pegs then carry branch state, not uptime. |

---

### Number of branches

| | |
|---|---|
| **Claim id** | `fleet.branches` |
| **Appears in** | Hero horizon segments, hero gauges |
| **Why it is needed** | The spine breaks into one segment per branch. Structure, not decoration. |
| **Current dev value** | 9 branches — **DEVELOPMENT PLACEHOLDER** |
| **Measurement date** | **required — not supplied** |
| **Source / verification** | **required — not supplied** |
| **Blocks implementation** | No |
| **Blocks public launch** | **Yes** |
| **Fallback if never supplied** | Draw a single unbroken spine and drop the branch labels. |

---

### The nine branch names

| | |
|---|---|
| **Claim id** | `fleet.branchNames` |
| **Appears in** | Hero horizon labels |
| **Why it is needed** | Named under each spine segment. Cities were chosen deliberately over client venues: naming a segment after Roshn or Boulevard would assert a deployment we cannot evidence. |
| **Current dev value** | Riyadh · Jeddah · Dammam · Khobar · Makkah · Madinah · Qassim · Abha · Tabuk — **DEVELOPMENT PLACEHOLDER** |
| **Measurement date** | **required — not supplied** |
| **Source / verification** | **required — not supplied** |
| **Blocks implementation** | No |
| **Blocks public launch** | **Yes** |
| **Fallback if never supplied** | Label segments by index (١–٩) with no place names. |

---

### Total transactions processed

| | |
|---|---|
| **Claim id** | `totals.transactions` |
| **Appears in** | Hero gauges |
| **Why it is needed** | The scale claim. |
| **Current dev value** | 465255 transactions — **DEVELOPMENT PLACEHOLDER** |
| **Measurement date** | **required — not supplied** |
| **Source / verification** | Carried from R.Pay's own production site |
| **Blocks implementation** | No |
| **Blocks public launch** | **Yes** |
| **Fallback if never supplied** | Remove the figure. The horizon still carries scale visually. |

---

### Prizes and gifts delivered

| | |
|---|---|
| **Claim id** | `totals.prizes` |
| **Appears in** | Fleet section |
| **Why it is needed** | The most human number the company owns. |
| **Current dev value** | 9434 prizes — **DEVELOPMENT PLACEHOLDER** |
| **Measurement date** | **required — not supplied** |
| **Source / verification** | Carried from R.Pay's own production site |
| **Blocks implementation** | No |
| **Blocks public launch** | **Yes** |
| **Fallback if never supplied** | Omit the line entirely. |

---

### Amount settled today

| | |
|---|---|
| **Claim id** | `totals.settledToday` |
| **Appears in** | Hero gauges |
| **Why it is needed** | A live-feeling figure. Also the most commercially sensitive number on the page. |
| **Current dev value** | _none — nothing is shown_ SAR — **DEVELOPMENT PLACEHOLDER** |
| **Measurement date** | **required — not supplied** |
| **Source / verification** | **required — not supplied** |
| **Blocks implementation** | No |
| **Blocks public launch** | **Yes** |
| **Fallback if never supplied** | Already applied: the gauge is omitted rather than filled with a plausible number. Decide whether it may be published at all. |

---

### Accepted payment methods

| | |
|---|---|
| **Claim id** | `rails.accepted` |
| **Appears in** | Hero rails row |
| **Why it is needed** | Answers the first question every operator has. |
| **Current dev value** | mada · VISA · Mastercard · Apple Pay · stc pay · GCCNET — **DEVELOPMENT PLACEHOLDER** |
| **Measurement date** | **required — not supplied** |
| **Source / verification** | Carried from R.Pay's own production site |
| **Blocks implementation** | No |
| **Blocks public launch** | **Yes** |
| **Fallback if never supplied** | Show only the methods that can be evidenced. The row degrades cleanly to any subset. |

---

### Customer and venue marks

| | |
|---|---|
| **Claim id** | `customers.logos` |
| **Appears in** | Where it runs (daylight) |
| **Why it is needed** | Third-party trademarks. Displaying them without written permission is a legal exposure, not a design decision. |
| **Current dev value** | _empty — the section is built to work without them_ — **DEVELOPMENT PLACEHOLDER** |
| **Measurement date** | **required — not supplied** |
| **Source / verification** | **required — not supplied** |
| **Blocks implementation** | No |
| **Blocks public launch** | **Yes** |
| **Fallback if never supplied** | SHIPPED BY DEFAULT: the section is built as a typographic register that carries deployment CLASS and city, and works with zero logos. Marks are additive, never structural. |

---

### Named customer deployments

| | |
|---|---|
| **Claim id** | `customers.namedDeployments` |
| **Appears in** | Where it runs (daylight) |
| **Why it is needed** | A named venue is a claim about somebody else's business. |
| **Current dev value** | _empty — the section is built to work without them_ — **DEVELOPMENT PLACEHOLDER** |
| **Measurement date** | **required — not supplied** |
| **Source / verification** | **required — not supplied** |
| **Blocks implementation** | No |
| **Blocks public launch** | **Yes** |
| **Fallback if never supplied** | Unnamed deployment classes only: arcade, vending, coffee, with city omitted. |

---

### Product name for the terminal

| | |
|---|---|
| **Claim id** | `hardware.name` |
| **Appears in** | The fleet section |
| **Why it is needed** | Naming the hardware turns a service company into one that makes a thing. Needs owner approval and KSA/GCC trademark clearance, classes 9 and 36. |
| **Current dev value** | وَتَد ٠١ / WATAD 01 — **DEVELOPMENT PLACEHOLDER** |
| **Measurement date** | **required — not supplied** |
| **Source / verification** | **required — not supplied** |
| **Blocks implementation** | No |
| **Blocks public launch** | **Yes** |
| **Fallback if never supplied** | Refer to it as 'the terminal' throughout. Costs nothing structurally. |

---

### Is R.Pay the region's largest arcade operator, or does it serve them?

| | |
|---|---|
| **Claim id** | `company.identity` |
| **Appears in** | Positioning throughout |
| **Why it is needed** | Earlier concepts contradict each other. These are opposite companies: if R.Pay IS the operator, every competing operator is being asked to fund a competitor. |
| **Current dev value** | _none — nothing is shown_ — **DEVELOPMENT PLACEHOLDER** |
| **Measurement date** | **required — not supplied** |
| **Source / verification** | **required — not supplied** |
| **Blocks implementation** | No |
| **Blocks public launch** | **Yes** |
| **Fallback if never supplied** | Already applied: the site claims neither. Copy describes what the system does, never who is largest. |

---

### CR number, VAT number, registered address, official Arabic name

| | |
|---|---|
| **Claim id** | `company.registration` |
| **Appears in** | Station ident (footer) |
| **Why it is needed** | Legally required on a Saudi commercial site, and the cheapest trust signal available. |
| **Current dev value** | _none — nothing is shown_ — **DEVELOPMENT PLACEHOLDER** |
| **Measurement date** | **required — not supplied** |
| **Source / verification** | **required — not supplied** |
| **Blocks implementation** | No |
| **Blocks public launch** | **Yes** |
| **Fallback if never supplied** | Footer renders without the registration block. It is built to omit, not to show blanks. |

---

### What is behind the control-room tour CTA

| | |
|---|---|
| **Claim id** | `commercial.tour` |
| **Appears in** | Primary CTA, all placements |
| **Why it is needed** | The page names a concrete 15-minute offer. Who runs it, on which days, with what response time. |
| **Current dev value** | _none — nothing is shown_ — **DEVELOPMENT PLACEHOLDER** |
| **Measurement date** | **required — not supplied** |
| **Source / verification** | **required — not supplied** |
| **Blocks implementation** | No |
| **Blocks public launch** | **Yes** |
| **Fallback if never supplied** | CTA becomes a WhatsApp conversation rather than a booked walkthrough. One string change. |

---

### WhatsApp number, sales phone, sales email, lead destination

| | |
|---|---|
| **Claim id** | `commercial.contact` |
| **Appears in** | Every CTA, station ident |
| **Why it is needed** | The conversion path has to land somewhere real. |
| **Current dev value** | _none — nothing is shown_ — **DEVELOPMENT PLACEHOLDER** |
| **Measurement date** | **required — not supplied** |
| **Source / verification** | **required — not supplied** |
| **Blocks implementation** | No |
| **Blocks public launch** | **Yes** |
| **Fallback if never supplied** | CTAs are inert in development and visibly marked as such. |

---

### What R.Pay offers a venue that HOSTS machines

| | |
|---|---|
| **Claim id** | `commercial.venueProposition` |
| **Appears in** | Who this is for (daylight) |
| **Why it is needed** | The second audience. Revenue share, footfall data, installation and maintenance terms. This content exists nowhere in the current site. |
| **Current dev value** | _none — nothing is shown_ — **DEVELOPMENT PLACEHOLDER** |
| **Measurement date** | **required — not supplied** |
| **Source / verification** | **required — not supplied** |
| **Blocks implementation** | No |
| **Blocks public launch** | **Yes** |
| **Fallback if never supplied** | Already applied: the section states only what the system verifiably does for a host and asks them to talk, rather than inventing commercial terms. |

---

### Privacy policy and PDPL position

| | |
|---|---|
| **Claim id** | `commercial.privacy` |
| **Appears in** | Station ident, lead form |
| **Why it is needed** | The contact form captures personal data under Saudi PDPL. |
| **Current dev value** | _none — nothing is shown_ — **DEVELOPMENT PLACEHOLDER** |
| **Measurement date** | **required — not supplied** |
| **Source / verification** | **required — not supplied** |
| **Blocks implementation** | No |
| **Blocks public launch** | **Yes** |
| **Fallback if never supplied** | The form is disabled until a policy exists. It does not collect anything in development. |


---

## Unresolved, but safe to ship

The fallback is already the shipped behaviour. Resolving these improves the page; not
resolving them costs nothing.


### Which specific machines are offline, and per-machine revenue

| | |
|---|---|
| **Claim id** | `fleet.machineBreakdown` |
| **Appears in** | Hero peg readouts on hover |
| **Why it is needed** | Drives the per-peg readout. Never published as a headline figure. |
| **Current dev value** | seeded distribution — **DEVELOPMENT PLACEHOLDER** |
| **Measurement date** | **required — not supplied** |
| **Source / verification** | **required — not supplied** |
| **Blocks implementation** | No |
| **Blocks public launch** | No |
| **Fallback if never supplied** | Already safe: the readout is labelled as illustrative and shows no branch-identifying data. |

---

### Terminal specifications

| | |
|---|---|
| **Claim id** | `hardware.specs` |
| **Appears in** | The fleet section |
| **Why it is needed** | Publishing an IP rating or EMV level that cannot be evidenced is a legal exposure. NOTHING is currently published. |
| **Current dev value** | _none — nothing is shown_ — **DEVELOPMENT PLACEHOLDER** |
| **Measurement date** | **required — not supplied** |
| **Source / verification** | **required — not supplied** |
| **Blocks implementation** | No |
| **Blocks public launch** | No |
| **Fallback if never supplied** | Already applied: no specification is stated anywhere. The section describes behaviour, not certification. |


---

## Resolved

_None yet._

---

## What is deliberately NOT on this list

Anything the site does not claim. The page states no hardware certification, no uptime
percentage, no regulator relationship, no customer count and no superlative, so none of
those appear here. The shortest way to keep a claim honest is not to make it.
