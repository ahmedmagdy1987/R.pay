import OneTapPage from "../concepts/one-tap/page";

/**
 * /meeting — presentation route.
 *
 * Renders the strongest finished concept in the repository (concept 08,
 * "One Tap. Total Control.") unchanged. Nothing is copied or forked, so the
 * original route keeps working and there is one source to fix.
 */
export default function MeetingPage() {
  return <OneTapPage />;
}
