"use client";

/** ACT III — THE THREE MACHINES. Horizontal snap rail.
 *  PHASE 1: shell. Cards (machine-vending/coffee/arcade.webp + one .led
 *  number + one line + text-link CTA) land in PHASE 2. */
export default function MachineRail() {
  return (
    <section className="act rail" id="machines" aria-label="Machines">
      {/* Phase 2: snap-scroll cards */}
    </section>
  );
}
